# Core
websockets>=12.0          # real-time market data feed
aiohttp>=3.9.0            # async HTTP
pandas>=2.0.0
numpy>=1.24.0

# Historical data (Hyperliquid S3 archives, requester-pays)
boto3>=1.34.0
lz4>=4.3.0

# Storage backends (each optional at runtime; enable per .env)
pyarrow>=14.0.0          # Parquet read/write (historical store + backtest loader)
sqlalchemy>=2.0.0
asyncpg>=0.29.0           # PostgreSQL async driver (used by create_async_engine)
influxdb-client>=1.40.0
redis>=5.0.0

# Order book reconstruction (sorted price levels for the L4/L2 book)
sortedcontainers>=2.4.0

# QuickNode Hypercore gRPC feed (L4 order stream; protos/orderbook.proto)
grpcio>=1.60.0
grpcio-tools>=1.60.0

# Scheduling
APScheduler>=3.10.0

# Configuration, CLI, logging
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
typer>=0.9.0
loguru>=0.7.0

# Charts (analysis example)
matplotlib>=3.7.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.23.0
